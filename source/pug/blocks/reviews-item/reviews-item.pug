mixin reviews-item(data)
    +el({
        tag: 'article',
        class: 'reviews-item',
        mix: data.mix,
        mod: data.mod,
        js: data.js,
    })&attributes(attributes)
        +el({
            class: 'reviews-item__image-wrap',
        })
            img.reviews-item__image(src='./assets/upload/' + data.image, alt='')
        .reviews-item__content
            +heading({
                tag: 'h3',
                mix: 'reviews-item__heading',
                heading: data.heading,
                subheading: data.subheading,
            })
            +el({
                class: 'reviews-item__text',
                text: data.text,
            })
                block
